"use client";
import React from 'react';

const Ultextgrid = () => {
  return (

    <div className='ulli-text-sec'>
      <div className='container'>
      <div className='ulli-text'>
          <div className="w-full grid grid-cols-1 md:grid-cols-2 gap-x-6 gap-y-10">
            <ul className="section_ulli max-w-[500px] list-disc text-[14px] 2xl:text-[22px] xl:text-[18px] md:text-[14px] sm:text-[14px] font-normal text-customwhite md:mb-4">
            <li>Draw attention to your call-to-action with an animated arrow or a quick burst of color.</li>
            <li>If you have content-rich pages, display page loading progress in a spinner so visitors won’t bounce before the page is fully loaded.</li>
            <li>Dynamically reveal page elements in the viewport as the user scrolls up or down the page.</li>
            <li>Improve the user experience by using simple animations like page swipes to make it clear when a user is taken to a new page.</li>
            </ul>
            <ul className="section_ulli max-w-[500px] list-disc text-[14px] 2xl:text-[22px] xl:text-[18px] md:text-[14px] sm:text-[14px] font-normal text-customwhite md:mb-4">
            <li>Draw attention to your call-to-action with an animated arrow or a quick burst of color.</li>
            <li>If you have content-rich pages, display page loading progress in a spinner so visitors won’t bounce before the page is fully loaded.</li>
            <li>Dynamically reveal page elements in the viewport as the user scrolls up or down the page.</li>
            <li>Improve the user experience by using simple animations like page swipes to make it clear when a user is taken to a new page.</li>
            </ul>
          </div>
        </div>
      </div>
    </div>

    );
  };

  export default Ultextgrid;